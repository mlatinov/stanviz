% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hierarchical.R
\name{plot_shrinkage}
\alias{plot_shrinkage}
\title{Shrinkage plot: raw vs. partially-pooled group means}
\usage{
plot_shrinkage(
  model,
  group_var,
  raw_means,
  grand_mean_var = NULL,
  group_labels = NULL,
  outcome_label = "outcome"
)
}
\arguments{
\item{model}{A fitted Stan model.}

\item{group_var}{Name of the group-level vector (absolute scale).}

\item{raw_means}{Numeric vector of length \code{J}: the raw per-group sample
means computed from your data.}

\item{grand_mean_var}{Optional name of the grand-mean scalar; if supplied,
the grand mean is drawn as a horizontal reference line.}

\item{group_labels}{Optional character vector of length \code{J}.}

\item{outcome_label}{Axis label.}
}
\value{
A \code{ggplot} object.
}
\description{
Visualises what partial pooling does. Plots raw per-group means against
partially-pooled posterior medians, with arrows showing how much each group
shrinks toward the grand mean.
}
\examples{
\dontrun{
raw <- tapply(df$y, df$group, mean)
plot_shrinkage(fit, "alpha_j", raw_means = raw, grand_mean_var = "mu")
}
}
