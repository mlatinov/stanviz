% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditional-effects.R
\name{plot_counterfactual_means}
\alias{plot_counterfactual_means}
\title{Counterfactual marginal means}
\usage{
plot_counterfactual_means(
  pred_draws,
  treatment_var = "treatment",
  outcome_label = "outcome",
  control_color = "lightblue4",
  treatment_color = "red4"
)
}
\arguments{
\item{pred_draws}{Long tibble of predictive draws.}

\item{treatment_var}{Name of the treatment indicator column.}

\item{outcome_label}{Y-axis label.}

\item{control_color}{Colour for the control arm.}

\item{treatment_color}{Colour for the treated arm.}
}
\value{
A \code{ggplot} object.
}
\description{
Side-by-side counterfactual marginal means under each arm.
}
\details{
Expects a long tibble of predictive draws with a treatment indicator
column (\code{0}/\code{1}) and a value column called \code{draw} (the per-draw mean for
that arm). You can build this from \code{\link[=extract_ate_from_draws]{extract_ate_from_draws()}} output with
\code{tidyr::pivot_longer()}.
}
\examples{
\dontrun{
ate_long <- extract_ate_from_draws(fit, "mu_treated", "mu_control") |>
  tidyr::pivot_longer(c(mu_control, mu_treated),
                      names_to  = "treatment",
                      values_to = "draw") |>
  dplyr::mutate(treatment = ifelse(treatment == "mu_treated", 1, 0))
plot_counterfactual_means(ate_long)
}
}
