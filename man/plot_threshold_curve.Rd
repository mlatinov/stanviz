% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parameter-posteriors.R
\name{plot_threshold_curve}
\alias{plot_threshold_curve}
\title{Probability the effect exceeds a threshold}
\usage{
plot_threshold_curve(
  model,
  par,
  favours_neg = TRUE,
  label = NULL,
  effect_units = "std. units"
)
}
\arguments{
\item{model}{A fitted Stan model.}

\item{par}{Single parameter name.}

\item{favours_neg}{Logical; if \code{TRUE}, plot \code{P(effect < c)}, else
\code{P(effect > c)}.}

\item{label}{Optional display label for the title.}

\item{effect_units}{String used in the x-axis label.}
}
\value{
A \code{ggplot} object.
}
\description{
Replaces the binary "is it significant" with a continuous "how sure are we
the effect is at least this big". \code{favours_neg = TRUE} plots \code{P(effect < c)}
(useful when reduction is the desired direction).
}
\examples{
\dontrun{
plot_threshold_curve(fit, par = "treatment_effect", favours_neg = TRUE)
}
}
