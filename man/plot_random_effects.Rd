% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hierarchical.R
\name{plot_random_effects}
\alias{plot_random_effects}
\title{Caterpillar plot of group-level effects}
\usage{
plot_random_effects(
  model,
  group_var,
  grand_mean_var = NULL,
  mode = c("absolute", "deviation"),
  group_labels = NULL,
  sort = TRUE,
  outcome_label = "group effect"
)
}
\arguments{
\item{model}{A fitted Stan model.}

\item{group_var}{Name of the group-level vector.}

\item{grand_mean_var}{Name of the grand-mean scalar (e.g. \code{"mu"}). Required
when \code{mode = "deviation"} and \code{group_var} is on the absolute scale.}

\item{mode}{Either \code{"absolute"} or \code{"deviation"}.}

\item{group_labels}{Optional character vector of length \code{J}.}

\item{sort}{Logical; sort groups by posterior median.}

\item{outcome_label}{String used in the x-axis label.}
}
\value{
A \code{ggplot} object.
}
\description{
Forest / caterpillar plot of every group's posterior, sorted by median.
Two display modes:
}
\details{
\itemize{
\item \code{mode = "absolute"}: shows the group-level estimates themselves
(e.g. \code{alpha_j}), with the grand mean as a reference line.
\item \code{mode = "deviation"}: shows deviations from the grand mean
(\code{alpha_j - mu}), centred on zero.
}

If \code{mode = "deviation"} and \code{group_var} already contains deviations (e.g.
you pass \code{"z"}), leave \code{grand_mean_var = NULL}.
}
\examples{
\dontrun{
# Absolute group-level intercepts
plot_random_effects(fit, "alpha_j", mode = "absolute",
                    grand_mean_var = "mu")

# Deviations from the grand mean
plot_random_effects(fit, "alpha_j", grand_mean_var = "mu",
                    mode = "deviation")
}
}
