% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditional-effects.R
\name{extract_ate_from_draws}
\alias{extract_ate_from_draws}
\title{Extract ATE draws from arm-specific Stan predictions}
\usage{
extract_ate_from_draws(model, mu_treated_var, mu_control_var)
}
\arguments{
\item{model}{A fitted Stan model.}

\item{mu_treated_var}{Name of the treated-arm expected outcome.}

\item{mu_control_var}{Name of the control-arm expected outcome.}
}
\value{
A \link[tibble:tibble]{tibble::tibble} with columns \code{.draw}, \code{mu_control},
\code{mu_treated}, \code{ATE}.
}
\description{
Given two generated-quantity variables representing \code{E[Y | treated]} and
\code{E[Y | control]}, returns a tibble of per-draw counterfactual means and
their difference (the ATE).
}
\details{
If either variable is per-observation (length \code{N}), it is averaged within
each draw to give the marginal mean for that arm.

Typical Stan-side setup:
\preformatted{
generated quantities \{
  vector[N] mu_treated = alpha + beta * 1 + ...;
  vector[N] mu_control = alpha + beta * 0 + ...;
\}
}
}
\examples{
\dontrun{
ate <- extract_ate_from_draws(fit, "mu_treated", "mu_control")
plot_ate_posterior(ate)
}
}
