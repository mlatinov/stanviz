% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combined-predictive-check.R
\name{plot_prior_posterior_check}
\alias{plot_prior_posterior_check}
\title{Prior + posterior predictive check, stacked}
\usage{
plot_prior_posterior_check(
  prior_model,
  model,
  y,
  prior_yrep_var = "y_rep",
  post_yrep_var = "y_rep",
  n_draws = 100,
  outcome_label = "outcome",
  ncol = 1
)
}
\arguments{
\item{prior_model}{A prior-only Stan fit (likelihood switched off), i.e.
the same object you'd pass to \code{plot_prior_predictive_dens()}.}

\item{model}{A fitted (posterior) Stan model, i.e. the same object you'd
pass to \code{plot_ppc_dens()}.}

\item{y}{Numeric vector of observed outcomes of length \code{N}. Used as the
comparison sample for both panels.}

\item{prior_yrep_var}{Name of the simulated-outcome vector family in the
prior fit's \verb{generated quantities}. Default \code{"y_rep"}.}

\item{post_yrep_var}{Name of the replicated-outcome vector family in the
posterior fit's \verb{generated quantities}. Default \code{"y_rep"}. Kept as a
separate argument from \code{prior_yrep_var} in case the two Stan programs
name it differently.}

\item{n_draws}{Number of draws to overlay per panel (passed through to
both underlying single-panel functions). Default 100.}

\item{outcome_label}{X-axis label, shared by both panels so they line up.}

\item{ncol}{Number of columns in the stacked layout. Default 1 (prior on
top, posterior below) - the natural reading order for "before/after
seeing the data." Pass 2 to place them side by side instead.}
}
\value{
A \code{patchwork} composite \code{ggplot} object with two panels: prior
predictive density overlay (top/left) and posterior predictive density
overlay (bottom/right), sharing an overall title.
}
\description{
Convenience wrapper that produces the prior predictive density overlay
(prior-only fit vs. observed sample) and the posterior predictive density
overlay (posterior fit vs. observed sample), and stacks them into one
patchwork figure. This is the "run everything at once" companion to
\code{plot_prior_predictive_dens()} and \code{plot_ppc_dens()} - use those directly
if you only want one panel or want to customize each independently.
}
\examples{
\dontrun{
plot_prior_posterior_check(prior_fit, fit, y = df$y)

# different generated-quantity names between the two Stan programs
plot_prior_posterior_check(
  prior_fit, fit, y = df$y,
  prior_yrep_var = "y_sim", post_yrep_var = "y_rep"
)
}
}
